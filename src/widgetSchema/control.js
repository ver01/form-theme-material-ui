const controlDefault = {
    component: null,
    children: [
        {
            mode: "editorHolder",
            component: null,
        },
    ],
};

export default {
    default: controlDefault,
};
